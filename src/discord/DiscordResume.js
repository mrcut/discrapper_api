const DiscordResume = ({ activerForm }) => {
  return (
    <div>
      <h3>Infos Du Discord</h3>
      <button className="btn btn-primary" onClick={activerForm}>
        Nouveau Discord
      </button>
    </div>
  );
};

export default DiscordResume;
